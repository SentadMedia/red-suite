language: node_js

node_js:
  - stable

services:
  - docker

cache:
  directories:
    - node_modules

jobs:
  include:
    - stage: Tests
      name: Unit Tests
      env:
        - CI=true
      script:
        - yarn
        - yarn test --passWithNoTests
